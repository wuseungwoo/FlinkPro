状态过期时间设置：
设置状态 TTL
状态还可以设置 TTL（Time To Live），来限制状态的有效时间，
状态过期后，存储的值会被自动清理，如果state是集合类型，
那么TTL是单独针对每个元素设置的，
也就是说每一个List元素、或者是Map的entry都有独立的TTL。

设置TTL后，默认会在读取时自动删除，如果状态配置了backend，则是后台进行垃圾回收。
也可以配置禁用后台垃圾回收。

